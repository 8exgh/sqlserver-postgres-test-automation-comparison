.env
.idea/
*.log
llm/
build/
lib/jdbc/
db/postgres/generated/

# AWS Schema Conversion Tool, 422 MB -- well over GitHub's 100 MB file limit, so
# it can never be pushed. scripts/convert-to-postgres.sh checks for it and prints
# the download URL if it is missing:
#   https://s3.amazonaws.com/publicsctdownload/jars/AWSSchemaConversionToolBatch.jar
lib/AWSSchemaConversionToolBatch.jar
lib/*.jar

# .NET build output for tests/DbParity.sln
bin/
obj/
app/build/
