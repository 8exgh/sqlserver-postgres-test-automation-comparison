{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Database": {
    "//": "SqlServer or Postgres. SQL Server is the default because it is this repository's source of truth; PostgreSQL is the port under test. Override with --Database:Provider=Postgres or Database__Provider=Postgres.",
    "Provider": "SqlServer",
    "LogSql": false
  },

  "//ConnectionStrings": "Left unset on purpose. When absent, DbParity.Core.TargetConfig supplies both strings from the same env-var / .env / docker-compose chain the parity suite and the bash scripts use. Set ConnectionStrings:SqlServer or ConnectionStrings:Postgres here to override."
}
